rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Anyone can read/write characters and visits (this is a public meme app)
    match /characters/{document} {
      allow read, write: if true;
    }
    match /visits/{document} {
      allow read, write: if true;
    }
  }
}
